<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Order Received - DentalKit</title>
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #0f172a; background: #f8fafc; margin: 0; padding: 24px; }
      .container { max-width: 640px; margin: 0 auto; background: #ffffff; border-radius: 12px; overflow: hidden; box-shadow: 0 2px 8px rgba(2,6,23,0.06); }
      .header { background: linear-gradient(135deg, #0ea5e9, #0369a1); color: #ffffff; padding: 28px 28px; text-align: center; }
      .content { padding: 28px; }
      .stat { display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #e5e7eb; }
      .stat:last-child { border-bottom: none; }
      .button { display: inline-block; background: #0ea5e9; color: #ffffff !important; padding: 12px 20px; text-decoration: none; border-radius: 6px; font-weight: 600; }
      .footer { text-align: center; padding: 20px; color: #64748b; font-size: 12px; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1 style="margin:0; font-size:24px;">New order received</h1>
        <p style="margin:8px 0 0; opacity:.95;">Order {{orderNumber}}</p>
      </div>
      <div class="content">
        <div class="stat"><span><strong>Customer</strong></span><span>{{customerName}} ({{customerEmail}})</span></div>
        <div class="stat"><span><strong>Total</strong></span><span>{{orderTotal}} EGP</span></div>
        <div class="stat"><span><strong>Items</strong></span><span>{{itemCount}}</span></div>
        <div class="stat"><span><strong>Date</strong></span><span>{{orderDate}}</span></div>
        <p style="margin:24px 0;"><a class="button" href="{{orderUrl}}">Open order</a></p>
      </div>
      <div class="footer">
        <p>&copy; {{year}} DentalKit. Admin notification.</p>
      </div>
    </div>
  </body>
</html>
